<UserControl x:Class="ui.MainGameScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ui"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--Section 1: progressbar-->
        <StackPanel Grid.Row="0" Margin="20,10">
            <TextBlock Text="Choose a side:" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
            <Grid Height="25" Margin="50,0">
                <ProgressBar x:Name="pbProgress" Height="25" Minimum="0" Maximum="100" Value="0" Foreground="LightGreen"/>
                <TextBlock x:Name="txtProgress" Text="0%" HorizontalAlignment="Center" FontSize="12" Foreground="Gray"/>
            </Grid>
        </StackPanel>

        <!--Section 2: Ranking area-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!--option A-->
            <Button x:Name="btnOptionA" Grid.Column="0" Click="btnOptionA_Click" Margin="10" BorderBrush="Gray">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="LightBlue"/>
                        <Setter Property="BorderBrush" Value="Gray"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="10">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="DeepSkyBlue"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="CornflowerBlue"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
                <StackPanel>
                    <Image x:Name="imageA" Height="400" Stretch="Uniform"/>
                    <TextBlock x:Name="textA" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="10"/>
                </StackPanel>
            </Button>

            <!--Middel buttons-->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="VS" HorizontalAlignment="Center" FontSize="30" FontWeight="Black" Margin="0, 20" Foreground="Red"/>
                <Button Content="Tie" Click="btnTie_Click" Margin="5" Padding="10, 5" Background="White"/>
            </StackPanel>

            <!--option B-->
            <Button x:Name="btnOptionB" Grid.Column="2" Click="btnOptionB_Click"  Margin="10" BorderBrush="Gray">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="LightSalmon"/>
                        <Setter Property="BorderBrush" Value="Gray"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">

                                    <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="10">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="Salmon"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="IndianRed"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
                <StackPanel>
                    <Image x:Name="imageB" Height="400" Stretch="Uniform"/>
                    <TextBlock x:Name="textB" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="10"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</UserControl>
