<UserControl x:Class="ui.StartScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ui"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="LightGray">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="20" BorderBrush="#DDD" BorderThickness="0,0,0,1">

            <TextBlock Text="Choose a category" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Border>
        <Grid Grid.Row="1" Margin="20">

            <ListBox x:Name="lbSubjects" SelectionChanged="lbSubjects_SelectionChanged" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="200" Margin="15">
                            <Border ClipToBounds="True" Height="100" CornerRadius="10">
                                <Image Source="{Binding Photo}" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" Margin="5"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>                              
        </Grid>

        <Border Grid.Row="2" Background="White" BorderBrush="#DDD" BorderThickness="0,1,0,0" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Subcategories:" FontWeight="Bold" FontSize="12" Margin="0,0,0,10"/>
                <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" MaxHeight="120">
                    <WrapPanel x:Name="wpFilters" Orientation="Horizontal"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <Border Grid.Row="3" Background="White" BorderBrush="#DDD" BorderThickness="0,1,0,0" Padding="20">
            <Grid>
                <Button x:Name="btnStartRanking" Content="Start Ranking" Width="200" Height="40" HorizontalAlignment="Center" Click="btnStartRanking_Click" IsEnabled="False"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
